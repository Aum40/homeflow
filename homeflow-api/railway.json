{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "pnpm build",
    "watchPatterns": ["homeflow-api/**"]
  },
  "deploy": {
    "startCommand": "pnpm db:migrate:deploy && pnpm start:prod",
    "healthcheckPath": "/health",
    "restartPolicyType": "ON_FAILURE"
  }
}
